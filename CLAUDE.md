# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 목표

자기소개 웹 페이지를 만듭니다. 소유자의 프로필, 기술 스택, 연락처를 보여주는 단일 페이지 정적 사이트입니다.

## 기술 스택

- 순수 HTML5, CSS3, 바닐라 JavaScript
- 빌드 도구, 프레임워크, 외부 라이브러리 없음
- `index.html`을 브라우저에서 직접 열어서 확인

## 파일 구조

```
day1/
├── index.html       # 단일 페이지 진입점
├── style.css        # 모든 스타일
└── script.js        # 인터랙션 (스크롤 효과, 테마 전환 등)
```

## 페이지 구성 (순서대로)

1. **Hero(상단)** — 이름, 직함/한 줄 소개, 프로필 사진, CTA 버튼 (예: "연락하기")
2. **소개(About)** — 짧은 자기소개 문단 (2~3문장)
3. **기술(Skills)** — 보유 기술을 태그 또는 카드 형태로 나열
4. **프로젝트(Projects)** — 2~4개의 프로젝트 카드: 제목, 설명, 링크
5. **연락처(Contact)** — 이메일 링크 또는 간단한 문의 폼 (백엔드 없이 `mailto:` 사용)

## 디자인 규칙

- 모바일 우선(mobile-first) 반응형 레이아웃 (CSS Flexbox 또는 Grid 사용)
- 색상과 간격은 `:root`에 CSS 사용자 정의 속성(변수)으로 정의
- 부드러운 스크롤: `html`에 `scroll-behavior: smooth` 적용
- 각 섹션에 앵커 이동용 `id` 속성 부여 (예: `#about`, `#skills`)
- 인라인 스타일 금지 — 모든 스타일은 `style.css`에 작성
- 인라인 스크립트 금지 — 모든 JS는 `script.js`에 작성하고 `defer`로 로드
